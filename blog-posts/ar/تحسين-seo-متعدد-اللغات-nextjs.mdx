---
title: 'تحسين SEO لمواقع متعددة اللغات بـNext.js (العربية والإنجليزية والتركية) 2026'
slug: تحسين-seo-متعدد-اللغات-nextjs
description: 'دليل Next.js App Router لـSEO متعدد اللغات — hreflang، روابط مُعرّبة، sitemap، وmeta لمواقع EN/AR/TR.'
date: '2026-04-24'
updated: '2026-04-24'
author: Mohamad Shahm
authorSlug: mohamad-shahm
authorRole: المؤسس ومهندس رئيسي
category: SEO
tags:
  - Next.js
  - SEO
  - i18n
  - متعدد اللغات
coverImage: https://images.unsplash.com/photo-1432888498266-38ffec3eaf0a?auto=format&fit=crop&w=1600&q=82
readingTime: 9
id: multilingual-seo-nextjs-guide
lang: ar
---

الموقع متعدد اللغات الذي يحقق ترتيبًا ليس ثلاث نسخ مترجمة من المحتوى نفسه. بل ثلاثة مواقع مستقلة تربطها hreflang، بروابط مُعرَّبة، وmeta أصيل لكل لغة، وخريطة موقع تُخبر Google بالضبط بأي نسخة يُقدّم لأي مستخدم.

<Callout tone="info" title="الجواب السريع">
لموقع Next.js بثلاث لغات في 2026: استخدم جزء `[locale]`، و**روابط مُعرّبة بالحروف الأصلية** (ليست إنجليزية لكل اللغات)، اربط الترجمات بمُعرّف قانوني مشترك، أصدر hreflang لكل صفحة، وابنِ sitemap يسرد كل نسخة مع lastmod صحيح. تخطي أيّ منها يفرّق قوة الروابط بين اللغات.
</Callout>

## الهندسة التي تحقق ترتيبًا

<Compare
  headers="النهج | جهد الإعداد | جودة SEO | الترتيب بالعربية/التركية"
  caption="أنماط ملاحظة عبر تدقيقات مواقع متعددة اللغات في الربع الأول 2026."
  topPickLabel="ما ننفّذه"
>
  <Row
    name="CloudTopia — Next.js متعدد اللغات"
    url="/website-design"
    verdict="جزء [locale]، روابط مُعرّبة، id قانوني، hreflang كامل، صور OG لكل لغة، بيانات مهيكلة لكل لغة."
    topPick
  >
    نظيف | ★★★★★ | ترتيب أصيل في كل نص
  </Row>
  <Row
    name="رابط إنجليزي + ?lang=ar"
    verdict="الأسوأ. Google قد يفهرس البارامتر، والمحتوى العربي يُبتلع في ترتيب الإنجليزية."
  >
    منخفض | ★ | بالكاد يظهر بالعربية
  </Row>
  <Row
    name="نفس الرابط الإنجليزي لكل اللغات"
    verdict="شائع وغير مثالي — الكلمات المفتاحية العربية/التركية في URL إشارة ترتيب قوية تضيّعها."
  >
    منخفض | ★★★ | مقبول لكن يفوّت فرصًا سهلة
  </Row>
  <Row
    name="نطاق فرعي لكل لغة (ar.site.com)"
    verdict="يعمل لكن إدارته أصعب، ويفرّق سلطة النطاق ما لم يُربَط جيدًا."
  >
    مرتفع | ★★★★ | يحقّق ترتيبًا لكن يحتاج روابط داخلية أكثر
  </Row>
</Compare>

## 1. جزء `[locale]`

في Next.js 14، ضع كل المسارات تحت `app/[locale]/`. middleware يكتشف اللغة ويحوّل عند الحاجة.

في التخطيط الجذر: `<html lang={locale} dir={locale === 'ar' ? 'rtl' : 'ltr'}>`. لا تضع `dir` على `body`.

## 2. روابط مُعرّبة مع مُعرّف قانوني

المفتاح: كل نسخة مترجمة تتشارك `id` قانونيًا في frontmatter، لكن كل لغة لها رابطها الأصلي.

```yaml
# blog-posts/en/gulf-payment-gateways.mdx
slug: gulf-payment-gateways-mada-stc-pay-tabby
id: gulf-payment-gateways-guide
lang: en
```

```yaml
# blog-posts/ar/بوابات-الدفع-الخليجية.mdx
slug: بوابات-الدفع-الخليجية-مدى-اس-تي-سي-باي-تابي
id: gulf-payment-gateways-guide
lang: ar
```

مساعد يحل الترجمات بـid:

```ts
export function getSlugById(id, locale) {
  return allPosts.find(p => p.id === id && p.lang === locale)?.slug
}
```

## 3. وسوم hreflang لكل صفحة

في `generateMetadata` لكل `page.tsx`:

```ts
const languages = {}
for (const locale of ['en','ar','tr']) {
  const slug = getSlugById(post.id, locale)
  if (slug) languages[locale] = `/${locale}/blog/${slug}`
}
return { alternates: { canonical, languages } }
```

## 4. خريطة الموقع

`app/sitemap.ts` يجب أن يسرد كل URL لكل لغة مع `lastModified` صحيح.

## 5. Meta لكل لغة — لا ترجمة

ترجم العناوين والأوصاف وصور OG **أصيلاً** لكل لغة. العنوان الإنجليزي المترجم يصبح 70 حرفًا بالعربية. أعد كتابته.

## 6. البيانات المهيكلة لكل لغة

BlogPosting وFAQPage وBreadcrumbList — كلها يجب أن تُصدَر بـ`inLanguage` مطابق.

## 7. الربط الداخلي داخل اللغة

لا تربط من مقال عربي إلى صفحة خدمة إنجليزية. ابقَ ضمن اللغة.

<Callout tone="warning" title="أسوأ خطأ في i18n لـNext.js">
استخدام نفس الرابط الإنجليزي للمقالات العربية والتركية. Google يرى `/ar/blog/gulf-payment-gateways` ولا يجد كلمات عربية في الرابط. ضيّعت إشارة ترتيب كبرى.
</Callout>

## قائمة التحقق

- [ ] جزء `app/[locale]/` مع middleware
- [ ] `<html lang dir>` من `params.locale`
- [ ] frontmatter يستخدم `id` قانوني + `slug` خاص بكل لغة
- [ ] مساعد `getSlugById` في meta ومبدّل اللغة
- [ ] `generateMetadata` يصدر `alternates.languages`
- [ ] `sitemap.ts` يتضمّن كل لغة × slug
- [ ] صور OG مُعرّبة
- [ ] `inLanguage` في البيانات المهيكلة يطابق الصفحة
- [ ] الروابط الداخلية تبقى داخل اللغة الحالية

<CTA
  title="تحتاج موقع Next.js متعدد اللغات يحقق ترتيبًا؟"
  description="نبني مواقع EN/AR/TR بـSEO مدمج من البداية — لا ملصق بعد الإطلاق."
  href="/website-design"
  label="خدمات التصميم"
/>

<FAQ title="أسئلة شائعة حول SEO متعدد اللغات في Next.js">
  <Q q="هل الروابط العربية والتركية بالحروف الأصلية أم مترجمة صوتيًا؟">
    الحروف الأصلية. ترتيب Google في الأسواق العربية يكافئ الكلمات العربية في الرابط. نفس الأمر للتركية — استخدم الإملاء التركي.
  </Q>
  <Q q="كيف أتعامل مع RTL وSEO في آن معًا؟">
    مستقلان. SEO عبر meta وhreflang؛ RTL شأن CSS/تخطيط. راجع [قواعد تصميم RTL](/ar/blog/قواعد-تصميم-الويب-العربي-rtl).
  </Q>
  <Q q="هل أحتاج خصائص Search Console منفصلة لكل لغة؟">
    اختياري. خاصية نطاق واحدة جيدة إذا كان hreflang صحيحًا.
  </Q>
  <Q q="ما الفرق بين hreflang='ar' وhreflang='ar-SA'؟">
    `ar` للعربية العامة، `ar-SA` حين تستهدف السعودية تحديدًا.
  </Q>
  <Q q="هل أستخدم next-intl أم إعداد i18n مخصّص؟">
    لـApp Router، `next-intl` يتعامل مع تحميل الرسائل جيدًا. لـmeta وخريطة الموقع ستكتب منطقًا مخصّصًا.
  </Q>
  <Q q="كيف يؤثّر dateModified على الترتيب متعدد اللغات؟">
    الحداثة مهمة لكل لغة. لا تنسخ تاريخ الإنجليزية إلى العربية.
  </Q>
  <Q q="هل أستخدم الترجمة الآلية لصفحات SEO؟">
    يمكنك، لكن النتائج متوسطة. استخدم الترجمة كمسودة؛ اطلب من ناطق أصيل إعادة الكتابة.
  </Q>
</FAQ>

## الخلاصة

SEO متعدد اللغات في Next.js ليس صعبًا لكنه غير متسامح. اضبط جزء `[locale]` والروابط الأصلية وid القانوني وhreflang وmeta لكل لغة من اليوم الأول.

<CTA
  title="بناء متعدد اللغات بشكل صحيح"
  description="مواقع EN/AR/TR بـSEO وhreflang وروابط مُعرّبة."
  href="/pricing"
  label="شاهد الأسعار"
/>
